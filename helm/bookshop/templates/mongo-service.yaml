apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.mongo.name }}
  labels:
    {{- include "bookshop.labels" . | nindent 4 }}
spec:
  selector:
    app: {{ .Values.mongo.name }}
  ports:
    - port: {{ .Values.mongo.containerPort }}
      targetPort: {{ .Values.mongo.containerPort }}
